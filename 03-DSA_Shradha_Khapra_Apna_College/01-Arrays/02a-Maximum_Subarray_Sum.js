/**
# Find maximum subarray Sum.

## Question:
1. Find a contiguous slice of an array that has the largest sum.
## Solution Approach:
*/
// -----------------------------

function maxSubArraySum(arr) {
  let maxSum = null;

  for (let i = 0; i < arr.length; i++) {}
  return maxSum;
}

// --- Output:
console.log(maxSubArraySum([1, 2, -2, 3, 4, -3, 1, 5, 4]));
