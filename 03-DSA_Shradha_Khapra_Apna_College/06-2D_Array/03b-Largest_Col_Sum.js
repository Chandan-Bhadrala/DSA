/**
# Find largest col sum.

## Question:
1. Find the col whose elements sum is the largest among all the cols.

## Solution Approach:
1. 
*/
// -----------------------------

// ## Function to find the col with the largest element sum.
function largestSumCol(matrix) {
  
}

// --- Output:
console.log(
  "Does target exists in the array? ",
  largestSumCol(
    [
      [1, 4, 7],
      [2, 5, 8],
      [3, 6, 9],
    ]
  )
);
