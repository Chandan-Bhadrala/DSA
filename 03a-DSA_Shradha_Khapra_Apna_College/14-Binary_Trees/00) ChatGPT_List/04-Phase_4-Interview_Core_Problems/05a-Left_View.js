/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 * this.val = (val===undefined ? 0 : val)
 * this.left = (left===undefined ? null : left)
 * this.right = (right===undefined ? null : right)
 * }
 */

/**
 * @param {TreeNode} root
 * @return {number[]}
 */

/**
## Using BFS
## Solution Approach:
1. I'll be scanning the tree level by level and I then I'll push the first element of the level into the resultant array.
2. Resultant array will be the my final array containing the elements from the Left-View.
*/

var leftView = function(root) {

      
};