/**
# Reverse an Array.
## Question:
  1. We will be given an array and we have to reverse all of its elements.
## Solution Approach:
  1. 
 */
function reverseArray(arr) {
  let reverseArr = [...arr];

  return reverseArr;
}
