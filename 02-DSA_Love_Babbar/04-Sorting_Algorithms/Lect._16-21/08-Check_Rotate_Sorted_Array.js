/**
# Check if rotated and sorted array.
## Question:
  1. We will be given an array and we have to check whether the given array is rotated and sorted or not.
  2. We have to return a boolean.
## Solution Approach:
  1. 
 */
function reverseArray(arr) {
  let reverseArr = [...arr];

  return reverseArr;
}
